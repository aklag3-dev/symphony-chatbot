{
  "name": "symphony-chatbot-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies chat requests to Gemini. The API key is stored as a Cloudflare secret, never in the browser.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "secret": "wrangler secret put GEMINI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
